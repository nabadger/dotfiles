#!/usr/bin/env bash
# Launch GNOME Settings under Sway (it refuses non-GNOME desktops otherwise)
set -euo pipefail
export XDG_CURRENT_DESKTOP=GNOME
export XDG_SESSION_DESKTOP="${XDG_SESSION_DESKTOP:-gnome}"
exec /usr/bin/gnome-control-center "$@"
