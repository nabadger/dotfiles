#!/bin/bash
# Native Wayland + PipeWire screen capture (required for multi-monitor share on Sway).
exec /usr/bin/google-chrome-stable \
  --ozone-platform=wayland \
  --enable-features=WaylandWindowDecorations,WebRTCPipeWireCapturer \
  "$@"
